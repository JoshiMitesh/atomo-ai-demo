## ATOMO Contracts

This package will contain:
- gRPC protobuf definitions (device control, telemetry, inference metadata)
- MQTT topic map + payload schemas
- Shared Zod schemas for REST and WS events

